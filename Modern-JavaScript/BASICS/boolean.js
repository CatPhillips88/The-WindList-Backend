// Data Type: Boolean

// Used as logic to test true or false values in programming
// Comparison operators are used to test conditions of boolean values

// Comparision operators
// == : loose equality
// === : strict equality
// != : loose inequality
// !== : strict inequality
// > : greater than
// < : less than
// >= : greater than or equal to
// <= : less than or equal to


let temp = 31;
// let isFreezing = temp === 31; // returns true
// let isFreezing = temp !== 31; // returns false

// let isFreezing = temp < 32; // returns true
// let isFreezing = temp > 32; // returns false

// let isFreezing = temp <= 32; // returns true
let isFreezing = temp >= 32; //returns false
console.log(isFreezing)

// CHALLENGE FOUR

// We want to offer discount to children and senior people at an amusement park
// Create an age variable to set age
// Calculate whether the age qualifies them as a child - 7 years or under
// Calculate whether the age qualifies them as a senior - 65 years or old

let age = 32;
let isChild = age <= 7;
let isSenior = age >= 65;

console.log(isChild)
console.log(isSenior)




